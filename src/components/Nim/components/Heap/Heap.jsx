import React, { Component } from 'react';
import './Heap.css';
// import PropTypes from 'prop-types';

export default class Heap extends Component {
  render() {
    return (
      <div className="Heap">
        <h2>This is a heap.</h2>
      </div>
    );
  }
};

Heap.prototypes = {
};
